function setup(){
  //initialize our canvas with width, height, and a render engine
  createCanvas(640, 480, P2D);
}

function draw(){
  //prove to ourselves that we've initialized a canvas by drawing a background
  background(255);
}