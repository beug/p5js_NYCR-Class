function setup(){
  //createCanvas(windowWidth,windowHeight);
}

function draw(){
  background('#654321');
  stroke(10);
  fill('#543210')
  ellipse(100,100,100);
  //rect(mouseX-50,mouseY-50,mouseX,mouseY);
}
